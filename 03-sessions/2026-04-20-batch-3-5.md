# Session — 2026-04-20 — Batch 3.5 (Graph Redesign Phase 1)

**Cowork session running Batch 3.5 Phase 1.** Branch `feature/graph-redesign-phase-1`; not pushed (sandbox has no GitHub credentials).

## Order of work

1. Read prompt, STATUS, OPERATING_RULES, BATCH_QUEUE. Noted the referenced `ark/01-architecture/graph-redesign-design-spec.md` does not exist in the vault (the directory is empty). Proceeded because all six design decisions are inlined in the prompt body itself — the missing spec file is a paperwork gap, not a decisions gap. Flagged at the end of this record.

2. Ran pre-change audit query against `graph_nodes`. Found 10 types (31,107 verse / 1,190 commentary / 184 theme / 58 person / 41 prophecy / 15 topic / 4 event / 4 place / 1 scholar / 1 manuscript) totalling 32,605 rows. Zero `feature_page`, `chapter`, `word`, or `strongs` nodes. Logged the full breakdown in `batch-3-5-audit.md` before touching any UPDATE.

3. Edge distribution: 28,645 verses (92.1% of all verses) have ≥5 outgoing edges. The auto-promote rule as written would blow past the prompt's 10,000 halt threshold. Held that item and pressed forward with everything else.

4. Applied Supabase migrations via MCP:
   - `037_graph_nodes_tiering` — columns + indexes
   - `038_graph_nodes_populate_tier` — tier by type (1,498 Tier 2, 31,107 Tier 4)
   - `039_graph_nodes_seed_significant_verses` — 128 explicit verses flagged, zero misses
   - `040_graph_nodes_seed_key_words` — no-op (0 strongs nodes exist); rule preserved in migration trail
   - `041_public_nodes_tiering_filter` — view now enforces tiering; count = 1,626

5. Built `src/lib/graph/graphTheme.ts`: testament-tone palette (OT amber `#D4A574`, NT teal `#5B9AA0`, intertestamental gold `#F5D06F`), shape registry per type (pentagon / hexagon / diamond / chevron / triangle / rectangle / outlined circle / rounded square / soft square / feature ring / small circle / default circle), tier base sizes with clamping, physics constants (alpha decay 0.03, velocity decay 0.4, link strength 0.3, charge -30, freeze threshold 0.01, interaction heat 2s), region anchor list (8 sections: Torah / Historical / Wisdom / Major Prophets / Minor Prophets / Gospels & Acts / Pauline / General & Rev), and a `paintShape` canvas helper.

6. Rewrote `NebulaGraph.tsx`:
   - `radiusOf` now calls `nodeRadius(tier, outgoing)` with the log-scaling + cross-tier clamp from the theme module.
   - `paintNode` uses `testamentColor(testament, type)` for the base color (Phase 1 upgrade path — Batch 3 swaps this out for the 66-book palette), `shapeForType(type)` for the outline, and `labelVisibleForTier` for label rules.
   - Added a settled-physics system: `reheatSimulation` wakes `d3Force` and schedules a 2-second re-freeze; `startFreezeWatcher` polls `simulation.alpha()` and calls `pauseAnimation` once it drops below 0.01. Mount effect applies link-strength / charge-strength via `d3Force` because `react-force-graph-2d`'s props only expose numeric decay.
   - `onRenderFramePre` paints a faint serif region-label backdrop computed from the centroid of each region's visible book-associated nodes; opacity ramps from 30% at k≤0.6 to 0 at k=1.5.
   - `onNodeDrag` and `onZoom` now call `reheatSimulation` so user interaction briefly wakes the layout.
   - Preserved Batch 2 behavior: single-click still opens the side panel (graph never navigates); Cmd/Ctrl-click focuses without navigate; double-click routes through `onNodeNavigate` (still a no-op at the page level); ESC / click-outside / URL `?focus=` unchanged.

7. Updated `/api/graph/edges` to pull `tier, is_significant_verse, is_key_word` from `public_nodes`.

8. Updated `GraphClient.tsx` to pass the three new columns from the API through to `GraphNode`, in both the primary mapping and the rawNodesById fallback used by side-panel edge target resolution.

9. Ran `npx tsc --noEmit` — 0 errors. Ran `npx eslint` on the four touched files — 0 errors. Did not run `npm run lint --fix` (forbidden by the batch prompt). Did not run `npm run build` (Cowork sandbox's virtiofs + Next.js mmap path still hits the bus-error limit Batch 2 documented).

10. Wrote `ark/batch-3-5-audit.md` (type breakdown, flag counts, view post-state, limitations), `ark/batch-3-5-blocker.md` (auto-promote threshold + Strong's absence), and this session record.

11. Plumbing commit on `feature/graph-redesign-phase-1` — same Windows-corrupt-index workaround as Batches 1.5, 1.5a, 2. No push (sandbox has no GitHub credentials — Marcus pushes from Windows).

## Halt reason (item 2 auto-promote only)

Prompt specifies `≥5 outgoing edges → auto-promote` for verses. Data shows 28,645 verses (92.1%) qualify. Applying the rule would push `public_nodes` to ~30,000 — above the prompt's own 10,000 halt threshold. Per the prompt's halt policy ("the data differs from what the design spec assumed") and `OPERATING_RULES §6`, halted before applying. Blocker filed with candidate thresholds; recommended `≥100 outgoing` which lands ~3,440 nodes (inside the 3,000–6,000 target).

## Missing-spec note

`ark/01-architecture/graph-redesign-design-spec.md` is referenced from both the Batch 3.5 prompt and `BATCH_QUEUE.md` but the directory `ark/01-architecture/` is empty. All six decisions the spec was meant to lock are inlined in the prompt — so the decisions themselves weren't lost — but the paperwork-of-record isn't in the vault yet. Flagging so Marcus can commit the source spec before Phase 2 queues.

## Files touched this session

Repo (`C:\Users\marcd\Downloads\MannaFest`):
- **NEW** `src/lib/graph/graphTheme.ts`
- `src/components/graph/NebulaGraph.tsx`
- `src/app/graph/GraphClient.tsx`
- `src/app/api/graph/edges/route.ts`

Vault (`C:\Users\marcd\Documents\MannaFest\ark`):
- **NEW** `ark/batch-3-5-audit.md`
- **NEW** `ark/batch-3-5-blocker.md`
- **NEW** `ark/03-sessions/2026-04-20-batch-3-5.md`
- `ark/STATUS.md` (decision-log entry appended)

Supabase:
- Migrations 037, 038, 039, 040, 041 applied via MCP.

## Open items

- Auto-promote threshold decision (blocker).
- Commit / push of `feature/graph-redesign-phase-1` from Windows.
- Vercel preview click-through against the acceptance criteria.
- Commit the actual design spec file into `ark/01-architecture/`.
